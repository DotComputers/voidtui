# Dependencies
node_modules/

# Bun binary lockfile (we use the text bun.lock)
bun.lockb

# macOS metadata
.DS_Store

# Environment / secrets
.env
.env.local
.env.*.local

# Build output
dist/
build/

# Logs
*.log

# SQLite runtime state (server.db and its WAL/SHM sidecars)
*.db
*.db-shm
*.db-wal

# Dev-scoped VOID_HOME convention
.void-dev/
